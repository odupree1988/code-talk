<ol class="mx-auto justify-content-center alert-primary" style="width: 500px;">
  {{#each posts}}
    <li class="text-center">
      <a href="/api/posts/{{id}}">{{title}}</a>,
      {{content}},
      {{user.username}},
      {{format_date created_at}}
    </li>
  {{/each}}
</ol>