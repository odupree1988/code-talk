<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Code Talk</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="stylesheets/style.css" />
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">Code Talk</a>
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarNavAltMarkup"
          aria-controls="navbarNavAltMarkup"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div
          class="collapse navbar-collapse justify-content-end"
          id="navbarNavAltMarkup"
        >
          <div class="navbar-nav">
            <a class="nav-item nav-link" href="/">Home</a>
            <a class="nav-item nav-link" href="/dashboard">Dashboard</a>
            {{#if loggedIn}}
              <button class="nav-item nav-link" id="logout">Logout</button>
            {{else}}
              <a class="nav-item nav-link" href="/login">Login</a>
            {{/if}}
          </div>
        </div>
      </nav>

      {{! <nav
        class="nav nav-pills flex-column flex-sm-row inline-flex justify-content-between"
      >
        <div class="container-fluid">
          <a class="flex-sm navbar-brand" href="/">Tech Blog</a>
        </div>
        <div class="container-fluid">
          <a class="nav-item nav-link" href="/">Home</a>
          <a class="nav-item nav-link" href="/dashboard">Dashboard</a>
          <button class="nav-item nav-link" id="logout">Link</button>
          <a class="nav-item nav-link" href="/login">Login</a>
        </div>
      </nav> }}
    </header>

    {{!-- <ul class="nav justify-content-center"></ul>
        <li class="nav-item">
          <a class="nav-link" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a href="/dashboard">Dashboard</a>
        </li>
        {{#if loggedIn}}
        <li class="nav-link">
          <button class="nav-link" id="logout">Logout</button>
        </li>
        {{else}}
        <li class="nav-item">
          <a class="nav-link" href="/login">Login</a>
        </li>
        {{/if}}
      </ul>
    </header> --}}
    <main>
      {{{body}}}
    </main>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
      crossorigin="anonymous"
    ></script>
    {{#if loggedIn}}
      <script src="/javascript/logout.js"></script>
    {{/if}}
  </body>
</html>